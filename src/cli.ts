#!/usr/bin/env node

import type { CliOptions } from "./types.js";

const main = (): void => {
  const _options: CliOptions = {
    dryRun: false,
    file: "",
    list: false,
    sectionPath: undefined,
  };
};

main();
